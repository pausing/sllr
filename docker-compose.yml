services:
  sllr:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - SLLR_DATA_DIR=/data
      - STATIC_DIR=frontend/dist
      - CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
    restart: unless-stopped
